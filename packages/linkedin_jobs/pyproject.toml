[project]
name = "linkedin-jobs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Mykyta Prokaiev", email = "mykytaprokaiev@gmail.com" }]
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.10.6",
    "selenium>=4.28.1",
    "sqlalchemy>=2.0.38",
]

[project.scripts]
linkedin-jobs = "linkedin_jobs:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
