[project]
name = "scrapers"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["linkedin-jobs"]

[tool.uv.sources]
linkedin-jobs = { workspace = true }

[tool.uv.workspace]
members = ["packages/*"]
